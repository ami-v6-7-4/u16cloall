language: clojure

lein:
  - 1.9.0
  
build:
      
  ci:
    - lein version
    - echo "(print *clojure-version*)"
    - clj clojure.main
    
integrations:
    
  hub:
 #Docker integration
    - integrationName: ship-docker
      type: dockerRegistryLogin
